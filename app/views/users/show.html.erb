<h1 class="text-center mt-4"><%= @user.first_name %>'s profile</h1>

#Gravatar stands for Globally Recognized Avatar. This web service lets users upload an online avatar and will associate the avatar with their email address.
#after login current user == user. user can edit their profile

<%= gravatar_for @user, size: 200 %>

<% if logged_in? && @user == current_user %>
  <div class="text-center mt-4">
    <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-info" %>
  </div>
<% end %>

<h3 class="text-center mt-4">Books</h3>
